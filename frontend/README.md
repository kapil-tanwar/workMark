# frontend — WorkFlow HR

React frontend for attendance & leave management.

## Run

```bash
npm install
npm run dev
```

Open http://localhost:8080/

## Backend API

The API lives in [`../backend/`](../backend/). The UI currently uses browser localStorage; set `VITE_API_BASE_URL=http://localhost:4000` in `.env` when wiring up the API.
